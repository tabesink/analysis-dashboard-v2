# DSC-19-05 — Editable schedule table and save UX

**Status:** DONE (2026-06-10)  
**Issue:** `docs/brainstorm/19_durability_sch_impl/issues/DSC-19-05-editable-save-ux.md`

## Summary

Completed editable durability schedule review: write users edit inline, Save via PUT, Reset discards local changes; read-only users see readonly table without Save.

## Hydration

- GET `parse_preview.event_rows` non-empty → `rowsFromSavedEventRows` (source of truth)
- Else → `buildDurabilityScheduleRows` from `entries` + events (extract path)
- Re-extract (new `schedule_id`) resets draft to freshly parsed values

## Page state (`database/edit/page.tsx`)

- `scheduleDraftRows` / `baselineScheduleRows` + multiplier + `delimiter_token`
- Dirty tracking via JSON compare + multiplier diff
- `handleSaveSchedule` → PUT → baseline sync from response
- `handleResetSchedule` restores baseline

## Table (`DurabilityScheduleTable.tsx`)

- `editable` + `onRowChange` + `onMultiplierChange` props
- Inline `<input>` cells (Workbench data-cell style)
- RSP File Name readonly; Global Multiplier schedule-level (same value per row in edit mode)

## Tests

`build-durability-schedule-rows.test.ts` — saved row round-trip helper test.
