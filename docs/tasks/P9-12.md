# P9-12 Dashboard Workspace Pruning Contract Hardening

## Status

DONE (2026-05-15)

## Summary

Implemented the next dashboard workspace stabilization slice by removing duplicated selection pruning from `LoadDataSection` and keeping pruning behavior owned by the dashboard workspace path fed by dimension-filtered selectable IDs.

## Changes

- Updated `client/src/hooks/use-event-catalog.ts` so `dimensionFilteredEventIds` includes only events with `selectable_for_plotting !== false`.
- Removed the side-panel-local pruning `useEffect` from `client/src/components/dashboard/side-panel/LoadDataSection.tsx` to avoid search-query-driven selection mutation.
- Added regression coverage in `client/src/modules/dashboard-workspace/dashboard-workspace.test.ts` for non-selectable-only persisted selections being pruned and render disabled.
- Updated `docs/master-build-plan.md` with task `P9-12`.

## Verification

- `npm run test -- src/modules/dashboard-workspace/dashboard-workspace.test.ts src/lib/api/client.test.ts` from `client/`
- `npm run lint -- src/hooks/use-event-catalog.ts src/components/dashboard/side-panel/LoadDataSection.tsx src/modules/dashboard-workspace/dashboard-workspace.test.ts` from `client/`
