# REF-12-01: Metadata PUT write guard

**Status:** DONE (2026-06-08)  
**Issue:** `docs/brainstorm/12_schedule_upload_clientside/issues/REF-12-01-metadata-put-write-guard.md`

## Change

- `update_program_version_metadata` in `server/routers/dashboard.py` now injects `WriteUserDep` instead of `CurrentUserDep`, matching `save_program_version_channel_map`.
- Service-layer ownership and admin-only `status` rules in `QueryService.update_program_version_metadata` unchanged.

## Tests

Added to `tests/server/routers/test_dashboard_router.py`:

1. Read-only registered user receives 403 with `"Write access required"` on metadata PUT.
2. Admin-created `can_write=true` user can batch-update own program/version metadata.

## Verification

```bash
cd server && uv run --extra dev pytest ../tests/server/routers/ -q
```

44 passed (2026-06-08).
