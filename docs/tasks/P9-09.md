# P9-09 Export/import Admin Guard Router Tests

## Status

DONE (2026-05-15)

## Summary

Implemented the refactor Slice B coverage for database portability routes. Export/import endpoints now have router-level tests that prove backend admin authorization is enforced independently of frontend visibility.

## Changes

- Added `tests/server/routers/test_export_router.py`.
- Covered unauthenticated rejection across the export/import task, download, cleanup, and start routes.
- Covered read-only and write-enabled non-admin rejection for the database info route.
- Added lightweight admin contract tests with a stub export service to avoid real DuckDB export/import work.
- Fixed the import-upload audit log field from reserved `filename` to `upload_filename`; the new admin upload route test covers this regression.

## Verification

- `uv run pytest ../tests/server/routers/test_export_router.py` from `server/`
- `uv run pytest ../tests/server/routers/test_auth_routes.py ../tests/server/routers/test_admin_users_router.py ../tests/server/routers/test_export_router.py` from `server/`
- `uv run ruff check routers/export.py ../tests/server/routers/test_export_router.py` from `server/`
