# PPU-27-06 — Harden precompute idempotency and document rollout

**Status:** DONE (2026-06-11)

## Summary

Closed the post-upload precompute loop with end-to-end idempotency tests across all automatic trigger points and expanded rollout documentation for future agents.

## Implementation

No new orchestrator behavior was required; existing `post_upload_precompute.py` decisions already enforce single active tasks, stale preservation, and schedule-only rescale fallbacks from PPU-27-01 through PPU-27-05. This slice adds regression coverage and documents the shipped boundaries.

## Tests

- `tests/server/services/test_post_upload_precompute_hardening.py`
  - Repeated channel reprocess completion and Inspect Damage access decisions reuse active damage tasks instead of creating duplicates.
  - Channel assignment before schedule: channel reprocess completes without damage; schedule save then starts and completes damage.
  - Inspect Damage repair: missing rows backfill converges to `damage_current` with persisted inspectable results.
  - Unscheduled uploaded events receive no `event_channel_damage` rows after damage calculation.
- `tests/server/routers/test_damage_router.py`
  - Repeated `POST /damage/backfill` reuses the active task (`reused_existing_task: true`).

Existing coverage retained for complementary acceptance criteria:

- `tests/server/services/test_post_upload_precompute.py` — decision matrix and schedule-before-channel e2e.
- `tests/server/services/test_schedule_damage_rescale.py` — stale/missing/error base damage rescale fallbacks.
- `tests/server/services/test_derived_data_task_hardening.py` — one active derived task per program/version.
- `client/src/features/inspect-damage/__tests__/plan-inspect-damage-backfill-attempts.test.ts` — client session dedupe.

## Documentation

- `docs/architecture/derived-data-upload-pipeline.md` — PPU-27 post-upload precompute section: trigger points, idempotency rules, damage constraints, workflow-order independence, follow-up exclusions, verification entry points.

## Notes

- Public derived task kinds remain `channel_reprocess` and `damage_calculation`.
- Read-only Inspect Damage no-op is enforced at the router (`WriteUserDep` on backfill) and covered by existing router tests.
