# DB14-06: Version-scoped durability schedule attachment

**Status:** DONE (2026-06-09)

## Summary

Added version-scoped durability schedule artifact storage with checksum, parse preview metadata, owner/admin attach permissions, event inheritance via program/version, and auditable replacement.

## Changes

- `server/services/durability_schedule.py` — autodam `.sch` parser, artifact storage, active attachment, audit logging.
- `server/schema.yaml` — `durability_schedule_artifacts`, `active_durability_schedules`, `seq_durability_schedule_id`.
- `server/storage/database.py` — upsert/list/active/get-for-event helpers; scope delete cleanup; portability exclusions.
- `server/routers/dashboard.py` — `POST/GET /api/v1/dashboard/program-version/schedule`.
- `server/models/dashboard.py` — attach/context response models.
- `server/dependencies.py` — `DurabilityScheduleStorageDep`.

## Tests

- `tests/server/services/test_durability_schedule.py` — parse, one-active-per-version, event inheritance, audit on replacement.
- `tests/server/routers/test_durability_schedule_router.py` — write/owner permission checks on attach API.

## Deferred

- Per-event schedule overrides and full schedule history manager (explicitly out of first release).
- Transfer package inclusion (DB14-07).
