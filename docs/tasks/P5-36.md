# P5-36: Database upload panel single drag/drop import control

Date: 2026-04-29
Status: DONE

## Goal

Make the Database page upload UI match the reference upload panel pattern: one dashed drag/drop card, normal multi-file picker on click, and a right-aligned Cancel/Import action row.

## Implementation

- Replaced the separate `Select Folder` and `Select Files` controls with one multi-file dropzone.
- Added drag-over/drop handling that forwards dropped files to the existing upload selection state.
- Preserved the current CSV/RSP/channel-map validation rules and metadata fields.
- Restyled the selected-file list to the compact reference pattern with per-file remove and Clear actions.
- Renamed the primary upload action to `Import` while keeping existing upload progress and cancel behavior.

## Verification

- `npx tsc --noEmit`
- `./node_modules/.bin/eslint "src/components/upload/UploadDataSection.tsx" --format json --output-file eslint-upload-panel-ui.json`
- IDE diagnostics for `client/src/components/upload/UploadDataSection.tsx` reported no linter errors.

## Files Changed

- `client/src/components/upload/UploadDataSection.tsx`
- `docs/master-build-plan.md`
- `docs/tasks/P5-36.md`
- `CHANGELOG.md`
