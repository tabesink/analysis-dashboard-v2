# DB14-02: Canonical CSV derivation and ingestion-run linkage

**Status:** DONE (2026-06-09)

## Summary

Added canonical CSV derived artifact storage and ingestion-run audit records so CSV and RSP uploads converge on the same downstream processing path with queryable event lineage.

## Changes

- `server/services/derived_artifact_storage.py` — stores canonical CSV under `artifact://canonical/` with SHA-256 checksums.
- `server/schema.yaml` — `derived_artifacts`, `ingestion_runs`, `event_ingestion_links` tables + sequences.
- `server/storage/database.py` — upsert/list/create helpers; scope delete clears derived ledger rows and filesystem bytes; portability excludes new lineage tables.
- `server/services/ingestion.py` — registers canonical CSV + ingestion run after source storage; links events to runs at commit.
- `docs/decisions/log.md` — DEC-074 canonical CSV and ingestion-run contract.

## Tests

- `tests/server/services/test_derived_artifact_storage.py` — canonical CSV storage behavior.
- `tests/server/services/test_ingestion_service_status.py` — RSP canonical derivation, CSV run linkage, scope-delete cleanup.

## Deferred to DB14-03+

- Event preview metadata derived from canonical CSV (DB14-04).
