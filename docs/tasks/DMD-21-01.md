# DMD-21-01 — Reuse the Assign Channels panel outside the route

Completed: 2026-06-10

## Summary

Extracted the Assign Channels tab UI and state from `client/src/app/database/edit/page.tsx` into `AssignChannelsPanel`, a scope-driven component mirroring `EditMetadataPanel`. The full-page route now renders the panel inside the existing tab; load, save, CSV preview, missing-channel-map footer, numeric input normalization, query invalidation, and write-permission gating are unchanged.

Shared channel-map constants live in `client/src/features/edit-metadata/lib/channel-map-constants.ts`.

## Key files

| File | Role |
|------|------|
| `client/src/components/edit-metadata/AssignChannelsPanel.tsx` | Scoped channel-map editor with `onDirtyChange` |
| `client/src/features/edit-metadata/lib/channel-map-constants.ts` | Fixed plot keys, table sizing, default draft |
| `client/src/app/database/edit/page.tsx` | Route tab delegates to panel |

## Tests

- `AssignChannelsPanel.test.tsx` — scoped render, clean dirty state, disabled save without column count, read-only affordance

## Follow-up

DMD-21-02 embeds the panel in `MetadataEditDialog`; DMD-21-03 hardens cross-section dirty-close and post-save table refresh.
