# REF37-10 — Server Orchestration Cleanup

## Behavior Added Or Changed

- Extracted dashboard channel-map and schedule route orchestration into `server/services/dashboard_orchestration.py`.
- Kept dashboard routes focused on request/response boundary concerns: auth dependency, request parsing, service invocation, and HTTP error translation.
- Preserved existing endpoint URLs and response models for:
  - `PUT /api/v1/dashboard/program-version/channel-map`
  - `POST /api/v1/dashboard/program-version/channel-map/upload`
  - `POST /api/v1/dashboard/program-version/schedule`
  - `PUT /api/v1/dashboard/program-version/schedule`
  - `GET /api/v1/dashboard/program-version/schedule`

## Interfaces Changed

- Added orchestration helper functions in `server/services/dashboard_orchestration.py`:
  - `require_uploaded_data_edit_permission`
  - `start_channel_reprocess_from_entries`
  - `start_channel_reprocess_from_yaml_upload`
  - `parse_schedule_preview`
  - `schedule_damage_extension`
- No public API contract changes and no route path changes.

## Tests Added And What They Prove

- No new tests added in this slice.
- Existing route tests remain the public behavior coverage anchor and were re-run focused on dashboard/upload/export routers to verify no regressions after extraction.

## Follow-On Assumptions For Next Slice

- Client module migration can rely on stable dashboard route contracts for channel-map and schedule commands.
- Server-side orchestration for these dashboard workflows now has a single helper module that can be expanded without inflating route handlers.

## Decisions Intentionally Left Unchanged

- Kept contributor ownership policy semantics unchanged.
- Kept damage precompute decision logic in `server/services/post_upload_precompute.py` and only delegated route orchestration/mapping to the new helper module.
- Did not introduce generic repository or ports/adapters layers.
