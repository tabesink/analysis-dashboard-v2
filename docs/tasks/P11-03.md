## P11-03: Backend metadata orchestration deepening and boundary tests

Date: 2026-03-30  
Status: DONE

### Goal
Move metadata mutation workflows out of router glue code into service boundaries, deduplicate weight-range domain rules, and add focused backend boundary tests.

### Implementation
- Added service-owned metadata mutation flows in `QueryService`:
  - `update_event_metadata(...)`
  - `update_program_version_metadata(...)`
  - shared helpers for normalization, username resolution, and cache invalidation.
- Simplified dashboard router metadata endpoints to request-map + error-map around service calls.
- Added `server/utils/weight_ranges.py` for shared derived-weight logic.
- Refactored ingestion to use shared weight derivation helper.
- Added `AuthService.get_user_from_token(...)` and updated dependency auth flow to avoid direct dependency-layer DB reach-through.
- Removed stale unused `UnifiedDatabase` protocol from `server/protocols.py`.

### Tests
- Added `tests/server/services/test_query_service_metadata.py`:
  - ownership guard for event metadata update
  - derived range + metadata update verification
  - program/version batch update summary behavior
- Added `tests/server/utils/test_weight_ranges.py`:
  - bucket mapping and derived-field behavior

### Verification
- IDE lint diagnostics report no issues in changed backend and test files.
- Test execution command attempted:
  - `python3 -m pytest tests/server/services/test_query_service_metadata.py tests/server/utils/test_weight_ranges.py`
  - blocked in environment: `No module named pytest`

### Files Changed
- `server/services/query.py`
- `server/routers/dashboard.py`
- `server/utils/weight_ranges.py`
- `server/services/ingestion.py`
- `server/services/auth.py`
- `server/dependencies.py`
- `server/protocols.py`
- `tests/server/services/test_query_service_metadata.py`
- `tests/server/utils/test_weight_ranges.py`
