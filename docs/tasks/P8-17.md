# P8-17 Parquet Import ZIP Path Safety

## Status

DONE (2026-05-15)

## Summary

Implemented the PR 3 data-safety slice for Parquet ZIP import extraction. Import validation and background import now reject unsafe archive member paths before writing files to the temporary extraction directory.

## Changes

- Added service-level regression tests for unsafe ZIP member validation and failed background import preserving current data.
- Added a shared ZIP member target guard in `server/services/export.py`.
- Replaced validation-time `shutil.unpack_archive` with guarded member extraction.
- Routed background import extraction through the same path guard while preserving progress updates and cancellation checks.
- Added `docs/refactor/CONCURRENCY_AND_DATA_SAFETY_REVIEW.md` with the PR 3 write-path inventory.

## Verification

- `uv run python -m pytest ../tests/server/services/test_export_service.py` from `server/`
- `uv run python -m pytest ../tests/server/services/test_export_service.py ../tests/server/services/test_ingestion_service_status.py ../tests/server/services/test_query_service_metadata.py` from `server/`
- `uv run ruff check services/export.py ../tests/server/services/test_export_service.py` from `server/`
