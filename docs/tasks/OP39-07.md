# OP39-07 - Lifecycle golden path observability

## Behavior changed

- Completed Phase 39 lifecycle verification as a focused regression gate rather than adding new runtime behavior.
- Confirmed lifecycle hardening remains coherent across supported operation lanes:
  - database import remains unavailable (`410 Gone`) while export/create/connect/delete continue to work for admins
  - failed-upload cleanup and retry guidance stays available with owner/admin cleanup authorization
  - folder upload polling remains creator-scoped
  - derived task polling remains admin-or-scope-owner authorized
  - database switch stays blocked by active operations and active users (with usernames)
  - active progress dialogs remain non-dismissible until terminal state; summaries remain closeable
- Confirmed observability payloads/log context remain sufficient without adding a diagnostics dashboard.

## Interfaces changed

- No API schema or UI interface changes were required in this slice.
- Documentation/contracts were finalized to reflect the validated lifecycle and observability surface.

## Tests added/updated

- Added `test_failed_folder_upload_cleanup_allows_reuploading_same_file_hash` in `tests/server/routers/test_upload_router.py` to explicitly verify the cleanup-and-retry golden path:
  - duplicate-hash re-upload fails while failed partial data remains
  - failed-task cleanup removes partial data/hash ownership
  - re-upload of the same file hash succeeds after cleanup
- Existing OP39-focused suites from OP39-01 through OP39-06 were re-run as the final golden-path gate.

Focused verification runs:

- `uv run pytest tests/server/routers/test_export_router.py tests/server/routers/test_upload_router.py tests/server/routers/test_dashboard_router.py tests/server/routers/test_damage_router.py tests/server/routers/test_auth_routes.py tests/server/unit/routers/test_upload_policies.py`
- `npm run test -- src/features/database-upload/UploadOperationModal.test.tsx src/components/upload/DatabaseOperationModal.test.tsx src/features/edit-metadata/__tests__/DerivedDataOperationModal.test.tsx` (from `client/`)

## GitNexus impact analysis

- Ran `impact({ repo: "analysis-dashboard-v2", target: "_build_upload_task_event", direction: "upstream", summaryOnly: true })`.
- Result: `risk=LOW`, `direct callers=2`, `impacted symbols=3`, `affected modules=Routers`.

## Residual risks / follow-ups

- Coverage is intentionally focused and modular; future operation kinds should extend the same status/blocker observability fields and add corresponding route/UI regression checks.
- Presence and admission guards remain intentionally in-process for this phase; multi-instance coordination remains out of scope.
