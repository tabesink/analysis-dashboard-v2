# AC-25-01 — Shell-mount channel reprocess modal and fix stacking

**Status:** DONE (2026-06-11)

## Summary

Moved derived-data operation modals (channel reprocess and damage calculation) from `MetadataEditDialog` to the Database page shell via `DatabaseDerivedDataOperationModals`, matching upload/scope-delete modal hosting. Applied shared `z-[70]` shell operation modal layering so progress stays above Edit Metadata (`z-50`).

## Changes

- Added `DatabaseDerivedDataOperationModals` on `client/src/app/database/page.tsx`.
- Removed duplicate `DerivedDataOperationModal` mounts from `MetadataEditDialog`; inline banners and store wiring unchanged.
- Added `client/src/lib/shell-operation-modal.ts` with shared layer classes; `DerivedDataOperationModal` and `AlertDialog` `containerClassName` use them.
- Extended `metadata-edit-dialog-store` with `requestMetadataEditDialogSection` so damage-calculation failure summary can still open the schedule editor after shell mount.

## Tests

- `DatabaseDerivedDataOperationModals.test.tsx` — shell mount and single modal instance.
- `MetadataEditDialog.test.tsx` — no derived-data modal markup inside editor when reprocess is active.
- `DerivedDataOperationModal.test.tsx` — shell z-index layer assertion.
