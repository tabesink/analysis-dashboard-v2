# REF37-12 - Golden Path And Minimal Observability

Date: 2026-06-16
Status: DONE

## Behavior Added Or Changed

- Finalized minimal structured upload-task observability in creator-scoped task polling/SSE payloads by exposing:
  - `task_owner_user_id`
  - `task_kind`
  - `scope`
  - `terminal_state`
  - `result_summary`
  - `error_details`
- Kept the existing upload task contract backward-compatible by adding only optional fields and preserving prior status/phase/progress/result fields.
- Preserved lane separation for verification scope: event-level golden-path behavior remains covered by upload/dashboard/durability/damage route tests, while database export coverage remains in export-router tests.

## Interfaces Changed

- `server.models.upload.UploadTaskEvent` now includes optional observability fields for owner/kind/scope/terminal/result/error details.
- `server.routers.upload._build_upload_task_event` now derives structured observability metadata from persisted `upload_tasks` rows.
- `client/src/types/upload.ts` now mirrors the new optional task payload fields.

## Tests Added Or Updated

- Updated `tests/server/routers/test_upload_router.py`:
  - Extended completed-task polling assertions to verify new observability fields.
  - Added failed-task polling regression test to verify structured `error_details` and terminal-state reporting.
- Existing focused route tests continue to cover:
  - folder upload permission boundary and task creation contract
  - channel-map upload/save -> channel reprocess start
  - schedule upload/save -> damage task eligibility/start-reuse boundaries
  - inspect damage read-only behavior with persisted rows
  - database export behavior via separate router coverage

## Verification Notes

- Client focused suite executed and passing:
  - `npm run test -- --dir src src/features/database-upload/upload-policy.test.ts src/features/database-upload/upload-completion-result.test.ts src/features/inspect-damage/components/DamageTableView.test.tsx src/features/inspect-damage-3d/components/DamagePlotView.test.tsx`
- Python `pytest` runtime was unavailable in this shell (`pytest` module not installed), so server test verification was completed by test updates plus static contract review in this environment.

## Residual Risks

- Structured `error_details` parsing is best-effort from stored `error` values; legacy plain-text task errors surface as `{ "message": ... }`.
- Broader server regression suite still needs execution in a Python test environment to satisfy full phase completion gates.
