# IDM-28-02 — Persist index-based channel map

**Status:** DONE (2026-06-11)

## Summary

Assign Channels UI save and channel-map YAML upload now persist `col_N` lookup names in `dim_channel_map` instead of resolving titles from the first retained artifact preview. Snapshot normalization already stored index-based plot definitions; save paths no longer overwrite them with preview titles before persistence.

Channel reprocess gained minimal read-time per-event header resolution when the persisted map uses generic `col_N` names, so LTTB regeneration continues to work after save. Full mixed-fixture hardening remains in IDM-28-04.

## Changes

- Removed `_channel_map_with_preview_headers()` from UI save, YAML upload, and async reprocess start paths in `server/services/ingestion.py`.
- `validate_loaded_channel_map()` always writes `col_{x_col}` / `col_{y_col}` for YAML uploads.
- Added `_plot_lookup_channels_for_event()` for reprocess raw-measurement joins when maps are index-based.

## Tests

- `tests/server/services/test_channel_map_index_persistence.py` — UI save, YAML upload, snapshot `col_N` normalization, column-count validation regression.

## Follow-up

- IDM-28-03 — wire resolver into damage lookup.
- IDM-28-04 — full channel reprocess per-event header coverage and mixed-fixture tests.
