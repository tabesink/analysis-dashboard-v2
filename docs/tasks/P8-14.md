# P8-14 Implementation Notes

Date: 2026-03-09
Status: DONE

## Scope

- Refactor Edit Metadata page UI to Database-style split-pane composition.
- Rename route from `/database/filter-values` to `/database/edit`.
- Preserve existing Filter Values edit/save behavior.
- Replace Custom Fields tab content with under-construction placeholder.

## Changes Made

### 1) Route migration with compatibility redirect

- Added new canonical route page:
  - `client/src/app/database/edit/page.tsx`
- Replaced legacy page with redirect:
  - `client/src/app/database/filter-values/page.tsx` -> redirects to `/database/edit`

### 2) Split-pane Edit Metadata layout

- Refactored Edit Metadata page into full-height, Database-style workspace:
  - Collapsible left control panel (`320px` expanded, rail when collapsed)
  - Dominant right content card with toolbar-style top strip
- Left panel now contains:
  - `Edit Metadata` header
  - Program ID and Version selectors
  - `Current Selection Summary` metadata block
  - clear selection action

### 3) Tab behavior updates

- `Filter Values` tab:
  - Preserved existing two-column metadata editor behavior.
- `Custom Fields` tab:
  - Removed prior custom-field editing UI.
  - Added placeholder using local static asset and message:
    - `client/public/images/edit-metadata-under-construction.gif`
    - text: `Under Construction`

### 4) Navigation and header wiring

- Updated sidebar route target:
  - `client/src/config/sidebar-config.ts`
- Updated route title mapping:
  - `client/src/config/header-config.ts`
- Updated nav special-case route checks:
  - `client/src/components/layout/NavMain.tsx`

## Validation

- Verified no remaining `/database/filter-values` references in client source routing config.
- Ran lint diagnostics on touched frontend files and addressed introduced issues.

## Notes

- Program ID/Version state remains local UI state (no URL query persistence), matching requested lean implementation.
