# REF-12-02: Invalidate filter-options after metadata save

**Status:** DONE (2026-06-08)  
**Issue:** `docs/brainstorm/12_schedule_upload_clientside/issues/REF-12-02-filter-options-invalidation.md`

## Change

- Added `invalidateQueriesAfterMetadataSave` in `client/src/lib/metadata-save-cache.ts` with `METADATA_SAVE_INVALIDATION_KEYS` including `filter-options`.
- `handleSave` in `client/src/app/database/edit/page.tsx` now calls the helper instead of six inline `invalidateQueries` calls.
- No change to save payload or server behavior.

## Tests

`client/src/lib/metadata-save-cache.test.ts` — behavior test confirms `filter-options` is invalidated along with existing metadata-dependent query keys.

## Verification

```bash
cd client && npm test -- src/lib/metadata-save-cache.test.ts
cd client && npm run build
```

Both passed (2026-06-08).

## Notes for REF-12-06

Reuse `invalidateQueriesAfterMetadataSave` in `useUpdateProgramVersionMetadata` when the save mutation hook lands.
