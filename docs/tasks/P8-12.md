## P8-12 Implementation Notes

Date: 2026-03-09
Status: DONE

## Scope

- Implement program-version metadata editing in Edit Metadata with RBAC behavior:
  - admin can edit all fields
  - non-admin can edit all non-status fields
- Persist edits to event metadata for the selected program/version.
- Update Selection Metadata panel fields to:
  - Last update by
  - Last update time
  - Uploaded by
  - Uploaded time
  - Status
- Make metadata fields schema/config-driven so they auto-surface in:
  - Database page columns (with show/hide support)
  - Global filters
- Use targeted refetch/invalidation after save.

## Notes

- Conflict strategy: last-write-wins.
- UI refresh strategy: server refetch after successful save.
- Custom fields expansion remains out of scope for this task.

## Implementation Summary

- Added backend bulk metadata endpoint:
  - `PUT /api/v1/dashboard/program-version/metadata`
  - role behavior: admin can update all fields; non-admin cannot update `status`
  - ownership behavior for non-admin: all target events must be owned by caller
- Added `last_updated_by_user_id` tracking:
  - schema/runtime support in `dim_event`
  - write on upload and metadata updates
- Updated Edit Metadata page save flow to update selected program/version event metadata (instead of updating global filter options).
- Updated Selection Metadata panel labels/values:
  - `Last update by`, `Last update time`, `Uploaded by`, `Uploaded time`, `Status`
- Updated Database page column model to include schema-driven metadata columns from filter options, with existing show/hide and per-column filter behavior preserved.
- Added targeted refetch/invalidation after save for event/filter-related queries.

## Validation

- `python3 -m compileall server` completed successfully.
- IDE lints for touched backend/frontend files reported no errors.
