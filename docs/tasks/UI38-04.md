# UI38-04 - Data and analysis surface standardization

## Behavior added or changed

- Standardized the `Database` and `Inspect Damage` non-plot surfaces around shared shadcn card/table/filter shells while preserving existing data operations.
- Updated damage-table informational and stale indicators to semantic token styling (`muted`/`destructive`) instead of hardcoded palette colors.
- Kept dashboard cards and damage-plot internals unchanged; only surrounding page/container/table treatment was standardized for this slice.

## Interfaces changed

- No API contract changes.
- No store or hook contract changes.
- Visual status badge helper (`getStatusBadgeClassName`) now emits semantic token classes for `Approved`, `Obsolete`, and default states.

## Verification

- Ran focused frontend tests:
  - `client/src/features/inspect-damage/components/DamageTableView.test.tsx`
  - `client/src/components/dashboard/side-panel/ComparisonLoadDataSections.test.tsx`
- Ran focused lint checks on edited frontend files; ESLint reported pre-existing `react-hooks/set-state-in-effect` findings in `DamageTableView` (not introduced by this slice).
- Ran GitNexus impact analysis before symbol edits:
  - `getStatusBadgeClassName` (CRITICAL upstream blast radius; change kept style-only/no status logic change)
  - `DamageTableView` symbol lookup returned unknown in current index, so edits were constrained to token-only class updates.

## Follow-on assumptions for next slice

- `UI38-05` can assume data/analysis pages now use standardized non-plot containers and semantic status surfaces.
- Existing sorting/filtering/selection/expansion behavior remains intact for database and damage table workflows.

## Decisions intentionally left unchanged

- No chart or plot card visual rework.
- No backend contracts or data semantics changed.
