# DSC-19-03 — Extract-to-review vertical slice

**Status:** DONE (2026-06-10)  
**Issue:** `docs/brainstorm/19_durability_sch_impl/issues/DSC-19-03-extract-review-ux.md`

## Summary

Wired the full extract → populate → review path on Database Edit using the DSC-19-02 row matcher.

## Changes

| Area | Behavior |
|------|----------|
| `page.tsx` | `buildDurabilityScheduleRows` when schedule attached; `scheduleUploadKey` bump clears upload after successful extract; combined loading state for schedule + events |
| Durability tab | Explicit empty copy when 404/no schedule; loading spinner while fetching; active schedule summary when attached |
| `DurabilityScheduleTable` | Removed `maxVisibleRows` data cap; scrollable body (`max-h-[min(60vh,640px)]`); optional min padding rows for short lists |

## Not in scope (DSC-19-04/05)

- Editable cells, Save button, PUT API
- Hydrate from saved `event_rows` on GET

## Manual QA

Upload `data/raw/13999/v58_data_processing/gmw17287_95per_bt1cc_iver.sch` for a program/version with matching RSP events; verify variant event names (`mf4e3_100`) and file picker clears after Extract.
