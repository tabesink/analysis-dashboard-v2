# P17-01 — Settings Dialog Modal

**Status:** DONE (2026-06-09)  
**Phase:** 17 — Settings Dialog Modal  
**Source:** `docs/brainstorm/16_settings_dialogue/`

## Summary

Replaced the full-page `/settings/users` admin route with a global modal settings dialog. Left nav: **User Management** and **Database**. Right panel shows migrated content from the existing app.

## Files created

| Path | Purpose |
|------|---------|
| `client/src/stores/settings-dialog-store.ts` | `useSyncExternalStore` open/route state |
| `client/src/components/settings/SettingsDialog.tsx` | Radix dialog shell, 180px nav rail |
| `client/src/components/settings/panels/UserManagementSettingsPanel.tsx` | P12 user CRUD migrated from settings page |
| `client/src/components/settings/panels/DatabaseSettingsPanel.tsx` | Transfer Data section + modals |
| `client/src/hooks/use-database-switch.ts` | Shared create/connect dialog logic |
| `client/src/components/settings/SettingsDialog.test.tsx` | Component tests |

## Files edited

| Path | Change |
|------|--------|
| `client/src/app/providers.tsx` | Mount `<SettingsDialog />` globally |
| `client/src/components/layout/AppSidebar.tsx` | Settings button opens dialog; label "Settings" |
| `client/src/app/settings/users/page.tsx` | Thin redirect → dashboard + open dialog |
| `client/src/app/database/page.tsx` | Use `useDatabaseSwitch` hook |
| `client/src/hooks/index.ts` | Export `useDatabaseSwitch` |

## Verification

```bash
cd client && npx vitest run src/components/settings/SettingsDialog.test.tsx
cd client && npm run build
```

Manual: admin opens Settings → User Management table; switch to Database → transfer actions; pending-user dot clears; `/settings/users` bookmark works.

## Decisions

See DEC-071 in `docs/decisions/log.md`.
