# IDM-28-04 — Channel reprocess uses per-event headers

Type: AFK

Blocked by: IDM-28-01, IDM-28-02

User stories covered: 5, 6, 13, 27, 35

## What to build

Wire the shared resolver into channel reprocess raw-measurement plot extraction so LTTB regeneration resolves `x_channel` / `y_channel` per artifact/event instead of using one version-wide title pair.

Align reprocess semantics with upload-time column-index behavior and with the damage lookup path from IDM-28-03.

## Acceptance criteria

- [ ] Mixed fixture batch regenerates LTTB plots for every event under one index-based map.
- [ ] Reprocess no longer depends on first-artifact preview titles for lookup joins.
- [ ] Empty plot extraction for an event produces clear failure/processing semantics consistent with existing task behavior.
- [ ] Server tests cover both naming conventions without rerunning full folder upload.

## Blocked by

IDM-28-01 and IDM-28-02.
