# IDM-28-03 — Damage lookup uses per-event headers

Type: AFK

Blocked by: IDM-28-01, IDM-28-02

User stories covered: 1, 2, 4, 9, 10, 12, 14, 23, 26, 38, 39

## What to build

Wire the shared resolver into schedule-driven damage input lookup so each event resolves plot axis channel names from **its own headers** before querying `measurements_raw`.

Replace version-wide exact title matching for damage channel series fetch. Preserve the fixed 12 Inspect Damage channels derived from 8 plot definitions. Persist actionable `error` rows when header resolution fails.

## Acceptance criteria

- [x] Mixed fixture batch (abbrev + full title conventions, same column layout) yields non-empty damage series for every event under one index-based map.
- [x] Damage task persists `current` rows for all schedule-matched events in mixed fixture tests.
- [x] Errors distinguish resolver/header failures from true empty measurement series where possible.
- [x] Existing single-convention tests continue to pass.
- [x] No change to scheduled damage formula or schedule validation rules.

## Blocked by

IDM-28-01 and IDM-28-02.
