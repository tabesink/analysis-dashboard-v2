# SPV-33-01 — Harden comparison state helpers and persistence

Type: AFK

Blocked by: None

User stories covered: 1, 4, 10, 11, 12, 32, 36

## Context packet

- `../PRD.md`
- `../HANDOFF.md`
- `../IMPLEMENTATION_MAP.md`
- `../../../../client/src/types/damage-comparison.ts`
- `../../../../client/src/lib/damage-comparison-state.ts`
- `../../../../client/src/lib/inspect-damage-table-preferences.ts`

## Previous slice provides

Recent frontend groundwork already provides:

- `DamageComparisonState` and related types in `client/src/types/damage-comparison.ts`.
- `getDefaultDamageComparisonState()`, `mergeDamageComparisonState()`, `pruneMissingEventIds()`, and `pruneMissingChannelKeys()` in `client/src/lib/damage-comparison-state.ts`.
- `mergeInspectDamageState()` integration so comparison state is defaulted/merged alongside inspect-damage table preferences.

## This slice changes

Harden the existing route-scoped damage-comparison state baseline so later UI and plot slices can rely on it.

The slice is vertical through type contract review, persistence/merge behavior, pruning helpers, any missing helper adapters, and focused tests. It should establish the comparison contract without wiring the full side-panel UI or plot rendering.

## This slice must not rework

- Existing single-pool dashboard Load Data state.
- Existing inspect-damage table preferences or layout persistence.
- The existing `DamageComparisonState` shape unless the PRD has changed.
- Plot aggregation math beyond minimal fixtures needed to prove state helpers.
- Backend APIs.

## Next slice can assume

- A default `DamageComparisonState` exists.
- Comparison state can be read, partially updated, merged, and persisted without dropping unrelated session fields.
- Missing event IDs and channel keys can be pruned from comparison state using the current catalog/channel list.
- Any needed program/version derivation helper exists, or downstream slices know exactly which existing catalog helper to use without persisting program/version as primary truth.

## Acceptance criteria

- [x] Existing comparison state type matches the PRD contract: Reference/Target selected event IDs, selected channel keys, value mode, and aggregation scope.
- [x] Default comparison state contains empty Reference/Target selections, an explicit selected-channel list, `absolute` value mode, and `selected_only` aggregation scope.
- [x] Partial comparison updates merge without dropping existing Reference, Target, channel, mode, aggregation scope, or unrelated route/session state.
- [x] `mergeInspectDamageState()` preserves table preferences while defaulting/merging comparison state.
- [x] Refresh/navigation restores Reference event IDs, Target event IDs, selected channel keys, and value mode.
- [x] Missing event IDs and missing channel keys are pruned deterministically when catalog/channel inputs change.
- [x] Program/version scope derivation is either implemented and tested, or explicitly deferred to the event-tree/raw-fact slice with the source helper named in `../IMPLEMENTATION_MAP.md`.
- [x] Existing dashboard and inspect-damage session behavior is covered by regression tests.

## Blocked by

None - can start immediately.
