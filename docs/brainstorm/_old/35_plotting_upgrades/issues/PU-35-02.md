# PU-35-02 — Move plot inputs into Inspect Damage sidepanel

Type: AFK

Blocked by: `PU-35-01`

User stories covered: 6, 7, 20, 26

## Context packet

- `../PRD.md`
- `../HANDOFF.md`
- `../IMPLEMENTATION_MAP.md`
- `PU-35-01.md`
- `../../../../client/src/app/inspect-damage/page.tsx`
- `../../../../client/src/features/inspect-damage-3d/components/DamagePlotOverlayControls.tsx`
- `../../../../client/src/features/inspect-damage-3d/lib/damage-channel-axis.ts`

## Previous slice provides

- Reference and Target selections are constrained to one program/version scope per side.

## This slice changes

Add a compact sidepanel Plot Inputs section below Target Load Data, separated by a divider. Move selected damage channel controls and value mode controls into this section.

The section should update `DamageComparisonState.selected_channel_keys` and `DamageComparisonState.value_mode`.

## This slice must not rework

- Main plot card rendering.
- Damage scale control.
- Table column visibility popover.
- Dashboard route sidepanel.
- Existing aggregate math.

## Next slice can assume

- Plot channel selection is available in the sidepanel and persisted through comparison state.
- Value mode is controlled outside the plot renderer.
- The old overlay rail is no longer needed for channels or value mode.

## Acceptance criteria

- [x] Sidepanel renders `Plot Inputs` below Target Load Data with a divider.
- [x] Channel chips/toggles update `selected_channel_keys`.
- [x] At least one selected channel is enforced.
- [x] Value mode segmented control updates `value_mode`.
- [x] Existing table `Cols` visibility remains table-only and does not mutate plot channel state.
- [x] Empty/missing damage states still allow Plot Inputs to be used.
- [x] Component tests cover channel toggles and value mode changes.

## Blocked by

- `PU-35-01`
