# PU-35-05 — Build cumulative-by-channel 2D plot spec

Type: AFK

Blocked by: `PU-35-02`, `PU-35-04`

User stories covered: 12, 18, 20, 21, 24, 31

## Context packet

- `../PRD.md`
- `../HANDOFF.md`
- `../IMPLEMENTATION_MAP.md`
- `PU-35-02.md`
- `PU-35-04.md`
- `../../../../client/src/features/inspect-damage/lib/build-damage-comparison-aggregates.ts`
- `../../../../client/src/features/inspect-damage/lib/build-damage-comparison-view-model.ts`
- `../../../../client/src/features/inspect-damage-3d/lib/build-comparison-damage-plot-cells.ts`

## Previous slice provides

- Sidepanel owns channel/value mode controls.
- Shared damage scale transform exists.

## This slice changes

Add the first pure 2D plot-spec builder path for `cumulative_by_channel`.

The spec should read existing comparison aggregates, filter selected channels, apply value mode, apply damage scale, and return grouped Reference/Target series with semantic colors and card labels.

## This slice must not rework

- Aggregate builder semantics.
- 3D cell builder behavior.
- SVG rendering.
- Plot grid layout.

## Next slice can assume

- A tested `Damage2DPlotSpec` exists for cumulative damage by channel.
- The spec contains categories, series values, legend, subtitle, warnings, and empty-state information.

## Acceptance criteria

- [ ] `buildDamage2DPlotSpec` or equivalent handles `cumulative_by_channel`.
- [ ] Spec values match aggregate channel totals for Reference and Target.
- [ ] Selected channel filtering is respected.
- [ ] Absolute and normalized modes produce expected values.
- [ ] Linear/log scale mode is reflected in values or domain consistently.
- [ ] Reference/Target colors are semantic and stable.
- [ ] Empty states produce renderable empty spec or null with explicit reason.
- [ ] Unit tests cover values, labels, colors, channel filtering, value mode, scale mode, and empty states.

## Blocked by

- `PU-35-02`
- `PU-35-04`
