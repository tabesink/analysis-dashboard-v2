# PU-35-04 — Add shared damage scale transform and in-plot toggle

Type: AFK

Blocked by: `PU-35-02`

User stories covered: 8, 19, 28, 30

## Context packet

- `../PRD.md`
- `../HANDOFF.md`
- `../IMPLEMENTATION_MAP.md`
- `PU-35-02.md`
- `../../../../client/src/features/inspect-damage-3d/components/DamagePlotView.tsx`
- `../../../../client/src/features/inspect-damage-3d/lib/damage-plot-overlay-types.ts`

## Previous slice provides

- Channels and value mode no longer depend on the overlay rail.

## This slice changes

Extract shared damage scale transformation and render a subtle Normal/Log toggle inside the main Inspect Damage plot area. The toggle should not live in the page header or sidepanel.

The existing inline `Math.log10(1 + cell.damage)` transform should move to a shared utility used by 3D and future 2D specs.

## This slice must not rework

- Plot card grid layout.
- 2D spec builder.
- Sidepanel channel/value controls.
- Damage calculation or aggregate values.

## Next slice can assume

- `applyDamageScale(value, mode)` or equivalent exists.
- The current focused 3D path uses the shared transform.
- A local `damageScaleMode` state is available to pass into 2D spec builders.

## Acceptance criteria

- [x] Shared damage scale utility supports `linear` and `log`.
- [x] Log mode uses `log10(1 + value)`.
- [x] Non-finite or negative values are handled consistently with existing renderer expectations.
- [x] Main plot area renders a subtle Normal/Log toggle.
- [x] Existing 3D plot output still honors linear/log scale.
- [x] Tests cover linear/log transform behavior.

## Blocked by

- `PU-35-02`
