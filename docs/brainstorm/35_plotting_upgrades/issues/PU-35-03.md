# PU-35-03 — Extract or introduce shared plot card shell

Type: AFK

Blocked by: `PU-35-00`

User stories covered: 2, 22, 25, 27

## Context packet

- `../PRD.md`
- `../HANDOFF.md`
- `../IMPLEMENTATION_MAP.md`
- `../references/ARCHITECTURE_AUDIT.md`
- `../references/REUSE_BLUEPRINT.md`
- `../../../../client/src/components/charts/SVGPlotCard.tsx`
- `../../../../client/src/components/charts/SVGPlot.tsx`
- `../../../../client/src/features/inspect-damage-3d/components/DamagePlotView.tsx`

## Previous slice provides

- Documentation establishes Dashboard-style card chrome as the visual target.

## This slice changes

Create a reusable `PlotCardShell` or damage-local equivalent that supports title, subtitle, loading, error, empty state, footer/action slots, and arbitrary renderer children.

Preferred path: extract common shell behavior from `SVGPlotCard` while keeping `SVGPlotCard` behavior unchanged. If extraction creates too much Dashboard churn, create a damage-local card shell matching the same styling and defer shared extraction.

## This slice must not rework

- SVG line renderer internals.
- Dashboard plot fetching.
- 2D damage chart renderers.
- Focused 3D routing.

## Next slice can assume

- Damage 2D and focused 3D cards can share consistent card chrome.
- Loading/error/empty copy can be rendered without duplicating card structure.

## Acceptance criteria

- [x] A card shell exists for arbitrary plot renderer children.
- [x] Existing `SVGPlotCard` tests and behavior still pass.
- [x] The shell supports bottom-left title/subtitle chip or equivalent Dashboard-style label.
- [x] The shell supports loading, error, and empty states.
- [x] The shell has no damage-specific data dependencies.
- [x] Component tests cover shell loading, error, empty, and normal children rendering.

## Blocked by

- `PU-35-00`
