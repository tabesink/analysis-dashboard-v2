# IDM-28-05 — Recovery, partial recalc policy, docs

Type: AFK

Blocked by: IDM-28-03, IDM-28-04

User stories covered: 11, 28, 32, 33, 34, 40

## What to build

Close the loop for real program/versions already saved with frozen title strings:

- Document operator recovery: re-save index-based map → channel reprocess → damage calculation.
- Evaluate and implement minimal policy fix so Calculate Damage / backfill can repair **partial** damage populations (some events `current`, others `error`/missing) after resolver fix, without duplicate active tasks.
- Update derived-data architecture doc to state column index + per-event headers as canonical lookup rule.

Add regression tests mirroring confirmed `002/v02`-style mixed cohort behavior.

## Acceptance criteria

- [x] Architecture doc updated with canonical lookup rule and deprecated first-file title persistence.
- [x] HANDOFF/recovery steps validated against mixed fixture integration test.
- [x] Partial-damage repair policy documented; write users can trigger full scheduled recalc when eligible events still lack displayable damage.
- [x] End-to-end test: mixed headers → save map → reprocess → damage → Inspect response shows displayable damage for all scheduled events.
- [x] No filename-based routing introduced.

## Blocked by

IDM-28-03 and IDM-28-04.
